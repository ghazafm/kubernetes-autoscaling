apiVersion: v1
kind: ConfigMap
metadata:
  name: hpa-flask-app-config
  namespace: default
  labels:
    app: hpa-flask-app
data:
  # Application settings
  APP_HOST: "0.0.0.0"
  APP_PORT: "5000"
  METRICS_PORT: "8000"
  DEBUG: "false"
  DEFAULT_SLEEP_TIME: "0.1"
  MAX_MEMORY_MB: "140"
  MAX_CPU_ITERATIONS: "500000"