# ═══════════════════════════════════════════════════════════════════
# K6 Load Testing Configuration
# ═══════════════════════════════════════════════════════════════════

# Target application URL (LoadBalancer IP or NodePort)
BASE_URL=

# ═══════════════════════════════════════════════════════════════════
# Replica Scaling Boundaries
# ═══════════════════════════════════════════════════════════════════
# CRITICAL: These MUST match agent/.env values for training consistency!
# k6 needs to stress the same replica range the RL agent will use.
# Mismatch = agent learns on different scale than deployment uses.

MAX_REPLICAS=
MIN_REPLICAS=

# ═══════════════════════════════════════════════════════════════════
# Pod Capacity Configuration
# ═══════════════════════════════════════════════════════════════════
# REQUESTS_PER_POD = concurrent requests each pod can handle safely
#
# Formula: (Workers × Threads) × Safety_Margin
#        = (3 × 4) × 0.66 = ~8
#
# Based on app/Dockerfile Gunicorn config:
#   - GUNICORN_WORKERS=3
#   - GUNICORN_THREADS=4
#   - Total capacity: 12 concurrent requests
#   - Safety margin: 0.66 (keeps pods at 60-80% utilization)
#
# RECALCULATE if you change Gunicorn workers/threads!
# Example: 4 workers × 8 threads × 0.66 = 21 requests/pod

REQUESTS_PER_POD=

# ═══════════════════════════════════════════════════════════════════
# Optional: InfluxDB configuration (send k6 metrics to InfluxDB)
# Mode A (InfluxDB v1 - built-in k6 support):
#   Set INFLUXDB_URL and INFLUXDB_DB. Example:
#     INFLUXDB_URL=http://localhost:8086
#     INFLUXDB_DB=k6_results
# Mode B (InfluxDB v2 - requires xk6-influxdb extension):
#   Set INFLUXDB_V2=true and provide token/bucket/org or use K6_INFLUXDB_* vars:
#     INFLUXDB_V2=true
#     INFLUXDB_URL=http://localhost:8086
#     INFLUXDB_BUCKET=my-bucket
#     INFLUXDB_TOKEN=your_token_here
#     INFLUXDB_ORG=my-org
# The runner will pick the appropriate mode and send metrics when configured.

# InfluxDB v1 example
#INFLUXDB_URL=
#INFLUXDB_DB=

# InfluxDB v2 example (xk6-influxdb)
#INFLUXDB_V2=true
#INFLUXDB_URL=
#INFLUXDB_BUCKET=
#INFLUXDB_TOKEN=
#INFLUXDB_ORG=

